package CH01_Strategy;

public interface QuackBehaviour {
    // Quack behaviour interface til at associere med duck abstrakt klassen så at alle duck har en eller anden form for quack
    void quack();
}
